<Window x:Class="IvanovItog.App.Views.RequestEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:props="clr-namespace:IvanovItog.App.Properties"
        Title="{x:Static props:Resources.RequestEditor_Title}"
        Height="400"
        Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Название" />
        <TextBox Grid.Row="1" Margin="0,10,0,0" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Grid.Row="2" Margin="0,10,0,0" Text="Описание" />
        <TextBox Grid.Row="3" Margin="0,10,0,0" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="120" TextWrapping="Wrap" />
        <StackPanel Grid.Row="4" Margin="0,10,0,0" Orientation="Horizontal">
            <StackPanel Width="140">
                <TextBlock Text="Категория" />
                <ComboBox Margin="0,5,0,0" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding CategoryId}" />
            </StackPanel>
            <StackPanel Margin="10,0,0,0" Width="140">
                <TextBlock Text="Статус" />
                <ComboBox Margin="0,5,0,0" ItemsSource="{Binding Statuses}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding StatusId}" />
            </StackPanel>
            <StackPanel Margin="10,0,0,0" Width="120">
                <TextBlock Text="Приоритет" />
                <ComboBox Margin="0,5,0,0" ItemsSource="{Binding Priorities}" SelectedValue="{Binding Priority}" />
            </StackPanel>
        </StackPanel>
        <Button Grid.Row="5" Margin="0,10,0,0" Content="Сохранить" Command="{Binding SaveCommand}" Height="40" />
    </Grid>
</Window>
