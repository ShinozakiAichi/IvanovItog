<Window x:Class="IvanovItog.App.Views.UserManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пользователи"
        Height="520"
        Width="720"
        MinHeight="460"
        MinWidth="680"
        WindowStartupLocation="CenterOwner"
        Loaded="OnLoaded">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top" Padding="8">
            <Button Content="Создать" Command="{Binding CreateUserCommand}" Margin="0,0,8,0" />
            <Button Content="Изменить" Command="{Binding EditUserCommand}" Margin="0,0,8,0" />
            <Button Content="Удалить" Command="{Binding DeleteUserCommand}" />
        </ToolBar>
        <Border Margin="16"
                Padding="16"
                CornerRadius="10"
                BorderBrush="#E0E6F2"
                BorderThickness="1"
                Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding DisplayName}" Header="Имя" Width="2*" />
                    <DataGridTextColumn Binding="{Binding Login}" Header="Логин" Width="*" />
                    <DataGridTextColumn Binding="{Binding Role}" Header="Роль" Width="*" />
                    <DataGridTextColumn Binding="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Header="Создан" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</Window>
