<Window x:Class="IvanovItog.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:props="clr-namespace:IvanovItog.App.Properties;assembly=IvanovItog.App"
        Title="{x:Static props:Resources.Login_Title}"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Вход" FontSize="20" FontWeight="Bold" />
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <TextBlock Text="Логин" />
            <TextBox Margin="0,5,0,0" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0,10,0,0" Text="Пароль" />
            <PasswordBox Margin="0,5,0,0" x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" />
        </StackPanel>
        <Button Grid.Row="2" Margin="0,20,0,0" Content="Войти" Command="{Binding LoginCommand}" Height="40" />
    </Grid>
</Window>
