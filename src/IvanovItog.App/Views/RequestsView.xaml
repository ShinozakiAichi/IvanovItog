<Window x:Class="IvanovItog.App.Views.RequestsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:props="clr-namespace:IvanovItog.App.Properties"
        Title="{x:Static props:Resources.Requests_Title}"
        Height="600"
        Width="900"
        Loaded="OnLoaded"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Обновить" Command="{Binding RefreshCommand}" />
            <Button Content="Очистить фильтры" Command="{Binding ClearFiltersCommand}" />
            <Button Content="Новая заявка" Command="{Binding CreateRequestCommand}" />
            <Button Content="Редактировать" Command="{Binding EditRequestCommand}" />
            <Button Content="Удалить" Command="{Binding DeleteRequestCommand}" />
            <Button Content="Назначить мне" Command="{Binding AssignToMeCommand}" />
            <Button Content="Закрыть" Command="{Binding CloseRequestCommand}" />
            <Button Content="Экспорт" Command="{Binding ExportCommand}" />
            <Separator />
            <Button Content="Рейтинг" Command="{Binding OpenRatingCommand}" />
            <Button Content="Аналитика" Command="{Binding OpenAnalyticsCommand}" />
            <Button Content="Настройки" Command="{Binding OpenSettingsCommand}" />
            <Separator />
            <Button Content="Выход" Command="{Binding LogoutCommand}" />
        </ToolBar>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <ComboBox Width="150" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedCategoryId}" />
                <ComboBox Width="150" ItemsSource="{Binding Statuses}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedStatusId}" />
                <ComboBox Width="150" ItemsSource="{Binding Priorities}" SelectedItem="{Binding SelectedPriority}" />
                <DatePicker SelectedDate="{Binding CreatedFrom}" Width="140" />
                <DatePicker SelectedDate="{Binding CreatedTo}" Width="140" />
                <TextBox Width="200" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <DataGrid Grid.Row="1"
                      Margin="0,10,0,0"
                      ItemsSource="{Binding Requests}"
                      SelectedItem="{Binding SelectedRequest, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="60" />
                    <DataGridTextColumn Binding="{Binding Title}" Header="Название" Width="200" />
                    <DataGridTextColumn Binding="{Binding Description}" Header="Описание" Width="200" />
                    <DataGridTextColumn Binding="{Binding Category.Name}" Header="Категория" Width="120" />
                    <DataGridTextColumn Binding="{Binding Priority}" Header="Приоритет" Width="100" />
                    <DataGridTextColumn Binding="{Binding Status.Name}" Header="Статус" Width="120" />
                    <DataGridTextColumn Binding="{Binding CreatedAt}" Header="Создана" Width="150" />
                    <DataGridTextColumn Binding="{Binding ClosedAt}" Header="Закрыта" Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
